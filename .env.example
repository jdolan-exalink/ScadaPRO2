# ===========================================
# ScadaPRO2 - Docker Compose Environment Variables
# 
# Copy this file to .env and adjust values
# for your specific deployment
# ===========================================

# ============================================
# Database Configuration
# ============================================
DB_USER=backend
DB_PASSWORD=backend_password_change_me_in_production
DB_NAME=industrial

# ============================================
# API Token
# ============================================
# Generate a secure token for API authentication
# On Linux/Mac: openssl rand -base64 32
# On Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { [byte](Get-Random -Maximum 256) }))
API_TOKEN=your_api_token_here_replace_with_secure_value

# ============================================
# Docker Images (Optional)
# ============================================
# Use these to override the default image names
# BACKEND_IMAGE=my-registry/scada-backend:latest
# FRONTEND_IMAGE=my-registry/scada-frontend:latest

# ============================================
# Frontend Environment (passed to build)
# ============================================
# Backend URL - used by frontend to connect to API
VITE_BACKEND_URL=http://localhost:8000
